{
  "name": "Spotify Listen Together Server",
  "description": "A server for the Spotify Listen Together extension.",
  "repository": "https://github.com/FlafyDev/spotify-listen-together-server",
  "keywords": ["spotify", "spicetify", "listen together"],
  "env": {
    "HOST_PASSWORD": {
      "description": "This password is required for a listener to receive host access.",
      "value": "1234",
      "required": false
    },
    "MAX_DELAY": {
      "description": "Most milliseconds the server will be willing to wait for all the listeners after an action.",
      "value": "5000",
      "required": false
    }
  }
}